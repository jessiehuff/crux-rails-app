<h2>All Tags:</h2>

<% if @tags.empty? %>
  <h3><%= "You don't have any tags yet. Please add one!" %></h3>
<% else %>
  <% @tags.each do |tag| %>
    <p><%= tag.name %></p>
  <% end %>
<% end %>

<br>
<h3>Add a New Tag:</h3>
<%= link_to 'New', new_tag_path %>

<br><br><br>
<%= link_to 'Back', projects_path %>
